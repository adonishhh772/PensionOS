# PensionOS Frontend (Next.js) - Minimal scaffold

Install dependencies and run the dev server from `frontend/`:

```bash
cd frontend
npm install
npm run dev
```

The scaffold includes example pages:
- `/` - basic index
- `/sources` - lists sources from backend API
